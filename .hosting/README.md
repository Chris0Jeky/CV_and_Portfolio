# Portfolio hosting reference

Reference-only preparation, 2026-09-10. `manifest.json` is inert agent-intake metadata. No public page, protected image, CV fact, domain, billing or deployment trigger changes here. This repository publishes main pushes; a future merge must be reviewed as a production-site action even when the diff is documentation only.

The existing static portfolio can become an umbrella entrypoint without renting an application server. CV1 prepares a product directory that distinguishes live apps, synthetic showcases, native downloads and planned work. Do not label every repository as a launched service. Keep private operational metadata and unregistered name/domain candidates out of this site.

CV2 prepares root/custom-domain base paths, canonical metadata and deep-link checks. Exercise the portfolio, every CV variant, case studies, scripts, styles and images from both direct and redirected entry. Test narrow layouts, keyboard access and refresh before cutover. Preserve the current GitHub Pages address until the new origin is verified.

CV3 reviews only approved public inputs. Never copy private HQ plans, contact ledgers, account details or unpublished dossiers. Preserve the protected headshot and factual CV content; no branding decision here grants permission to replace them.

Follow `AGENTS.md` and the existing relevant skills. The referenced root `CLAUDE.md` returned 404 during this pass; record that orientation drift rather than inventing its content or replacing the agent system. Syntax: `python -m json.tool .hosting/manifest.json`. Full page/link/mobile and hosted acceptance remain separate checks. No browser behavior is proven by JSON validation.

Rollback keeps the previous public build and functioning old links. Domain purchase, canonical hostname and any display-name changes remain explicit owner actions.
